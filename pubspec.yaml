name: stock_market_test
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  freezed_annotation: ^2.2.0
  cupertino_icons: ^1.0.2
  json_annotation: ^4.7.0
  fl_chart: ^0.55.2
  injectable: ^2.1.0
  get_it: ^7.2.0
  flutter_bloc: ^8.1.1
  dio: ^4.0.6
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner: ^2.3.3
  freezed: ^2.3.2
  json_serializable: ^6.5.4
  injectable_generator: ^2.1.3

flutter:
  uses-material-design: true
